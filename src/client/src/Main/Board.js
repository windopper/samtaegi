import './Board.css'

export default function Board(params) {

    const queue = params.queue

    return (
        <div className='board'>
            {/* <img src={queue[0].thumbnails} alt='no' className='thumbnail'/>
            <div className='title'>{queue[0].title}</div> */}
        </div>
    )
}